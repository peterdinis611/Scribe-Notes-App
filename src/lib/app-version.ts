/** Single source for UI chrome; keep in sync with package.json / Tauri. */
export const APP_VERSION = '2.3.0'

/** Major.minor for “Scribe 2.3” chrome. */
export const APP_SHORT_VERSION = APP_VERSION.split('.').slice(0, 2).join('.')
