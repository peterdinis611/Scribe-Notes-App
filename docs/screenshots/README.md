# Screenshots

App screenshots used in the root README files.

| File | Description |
|------|-------------|
| `icon.png` | App icon |
| `home.png` | Home / welcome screen |
| `templates.png` | New document · template picker |
| `onboarding.png` | First-run onboarding dialog |

Captured from the running UI (v1.4.4). To refresh:

```bash
npm run tauri:dev
# or: npm run dev  → open http://127.0.0.1:5174
# then save PNGs here and resize with:
sips -Z 1400 docs/screenshots/<name>.png --out docs/screenshots/<name>.png
```
