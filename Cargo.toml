[workspace]
members = ["src-tauri", "crates/scribe-core", "crates/scribe-mcp", "crates/scribe-agent"]
resolver = "2"

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
